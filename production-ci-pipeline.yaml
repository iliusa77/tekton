apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: production-ci
spec:
  params:
    - name: repo-url
      type: string

    - name: image
      type: string

  workspaces:
    - name: shared-workspace

  tasks:

    - name: clone
      taskRef:
        name: git-clone

      params:
        - name: url
          value: $(params.repo-url)

        - name: revision
          value: main

        - name: deleteExisting
          value: "true"

      workspaces:
        - name: output
          workspace: shared-workspace

    - name: test
      runAfter:
        - clone

      taskRef:
        name: run-tests

      workspaces:
        - name: source
          workspace: shared-workspace

    - name: build-image
      runAfter:
        - test

      taskRef:
        name: build-image

      params:
        - name: IMAGE
          value: $(params.image)

      workspaces:
        - name: source
          workspace: shared-workspace